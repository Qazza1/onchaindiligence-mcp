# Arc Testnet feedback

Current RPC is `https://rpc.testnet.arc.network`; older `.io` examples are stale. Deterministic finality is profile evidence. ERC-8004 response/reputation are registry evidence, never OCD `VALID` or safety scores. ERC-8183 `Completed` is protocol completion, not service-delivery proof. Beta feedback may earn points only when approved; testnet transactions are not documented as automatic points.
